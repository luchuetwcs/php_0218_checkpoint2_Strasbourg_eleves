{% extends 'layout.html.twig' %}

{% block content %}
    <div class="row">
        <h1 class="col-12">All Beasts</h1>
    </div>
    <div class="row">
        <ul class="list">
            {% for beast in beasts %}
              <form action="/beasts/{{ beast.id }}" method="post">
                <li><p>{{ beast.id }}. {{ beast.name }}</p></li>
                <input type="submit" name="detail" value="Détails">
                <input type="hidden" name="details" value="{{ beast.id }}">
              </form>
            {% else %}
                <li>Nothing to display</li>
            {% endfor %}
        </ul>
    </div>
    <nav class="row">
        <a href="/beasts/add">Add a beast</a>
        <a href="/movies">Check the movies</a>
        <a href="/planets">Check the planets</a>
    </nav>
{% endblock %}
